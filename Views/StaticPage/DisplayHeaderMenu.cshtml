@model IEnumerable<Todo.Site.Models.StaticPage>

@{
    ViewBag.Title = "Index";
}

@foreach (var item in Model) {
   <li> <a href="@Url.Action("Index","Page", new { id=item.PageId })">@Html.DisplayFor(modelItem => item.title)</a></li>
}
