@model IEnumerable<Todo.Site.Models.Page>

@{
    ViewBag.Title = "Index";
}

@foreach (var item in Model) {
   <li> <a href="@Url.Action("Index","Page", new { id=item.Id })">@Html.DisplayFor(modelItem => item.title)</a></li>
}
